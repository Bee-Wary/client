import { ReactNode } from 'react';

const BeehiveDetailPage = (
    { children } :
    { children? : ReactNode }
    ) => {

    return (
        <>
        {children}
        </>
    );
}

export default BeehiveDetailPage